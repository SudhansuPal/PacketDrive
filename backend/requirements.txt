# PacketDrive backend runtime dependencies.
# Pinned to the versions verified working on Python 3.11.
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1
websockets==16.0

# Optional: live packet capture. The backend falls back to simulated
# traffic when scapy (or the privileges it needs) is unavailable, so this
# is not required to run in simulate mode.
# scapy==2.6.1
